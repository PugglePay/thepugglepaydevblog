# The PugglePay dev blog

## Run locally:

``` sh
rake preview
```

## New post:

``` sh
rake new_post["Title"]
```

use `<!-- more -->` to split the intro and the rest of the post (the "read more..." part).

## Deploy

``` sh
make deploy
```
